# Linera Web packages

This directory contains bindings and utilities for using Linera from
the Web platform.

They are published on npm under the `@linera` namespace.
docs(readme): fix grammar in linera-rpc bullet (data type; tracks)
